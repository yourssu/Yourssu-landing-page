export interface OSType {
  type: string;
}

export interface IntroElement {
  title: string;
  description: string;
  divStyle: string;
  color: string;
}

export interface Culture {
  id: string;
  title: string;
  description: string;
}

export interface Ideal {
  title: string;
  description: string;
}
